import { HoldScrollDirective } from './hold-scroll';

describe('HoldScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new HoldScrollDirective();
    expect(directive).toBeTruthy();
  });
});
